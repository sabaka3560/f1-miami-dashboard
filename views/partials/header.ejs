<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700;900&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
</head>
<body>
    <nav class="floating-nav">
        <div class="nav-container">
            <div class="nav-brand">
                <span class="brand-icon">🏁</span>
                <span class="brand-text">F1 MIAMI</span>
            </div>
            <div class="nav-links">
                <a href="/" class="nav-link <%= currentPage === 'home' ? 'active' : '' %>">Home</a>
                <a href="/about" class="nav-link <%= currentPage === 'about' ? 'active' : '' %>">About</a>
                <div class="nav-dropdown">
                    <span class="nav-link dropdown-trigger">Dashboards ▼</span>
                    <div class="dropdown-menu">
                        <a href="/dashboard/drivers" class="dropdown-link <%= currentPage === 'drivers' ? 'active' : '' %>">Drivers</a>
                        <a href="/dashboard/circuits" class="dropdown-link <%= currentPage === 'circuits' ? 'active' : '' %>">Circuits</a>
                        <a href="/dashboard/standings" class="dropdown-link <%= currentPage === 'standings' ? 'active' : '' %>">Standings</a>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <main class="main-content">

